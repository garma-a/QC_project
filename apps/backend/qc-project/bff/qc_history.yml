info:
  name: "QC History"
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/bff/qc/history"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
