info:
  name: Dashboard
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/bff/dashboard"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
