info:
  name: "Get QC Result"
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/qc-results/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
