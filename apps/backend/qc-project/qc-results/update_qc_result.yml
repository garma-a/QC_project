info:
  name: "Update QC Result"
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/qc-results/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
