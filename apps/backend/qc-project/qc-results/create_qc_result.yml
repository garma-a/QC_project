info:
  name: "Create QC Result"
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/qc-results"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
