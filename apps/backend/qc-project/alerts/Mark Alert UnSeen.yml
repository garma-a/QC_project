info:
  name: Mark Alert UnSeen
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/alerts/mark-unseen/2465"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
