info:
  name: Mark Alert Resolved
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/alerts/mark-resolved/2465"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
