info:
  name: "Create Machine"
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/machines"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
