info:
  name: "Get Machine"
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/machines/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
