info:
  name: "Get Machines"
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/machines"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
