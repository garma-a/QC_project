info:
  name: "Update Machine"
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/machines/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
