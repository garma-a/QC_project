info:
  name: "Delete Machine"
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/machines/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
