info:
  name: "Create QC Test"
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/qc-tests"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
