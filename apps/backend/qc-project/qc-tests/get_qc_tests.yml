info:
  name: "Get QC Tests"
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/qc-tests"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
