info:
  name: Get QC Tests By Machine
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/qc-tests/machine/22"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
