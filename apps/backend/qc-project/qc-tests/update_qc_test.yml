info:
  name: "Update QC Test"
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/qc-tests/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
