info:
  name: "Create Control Lot"
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/control-lots"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
