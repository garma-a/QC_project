info:
  name: "Update Control Lot"
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/control-lots/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
