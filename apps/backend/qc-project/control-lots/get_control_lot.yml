info:
  name: "Get Control Lot"
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/control-lots/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
