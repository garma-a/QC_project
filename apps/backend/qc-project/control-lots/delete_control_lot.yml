info:
  name: "Delete Control Lot"
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/control-lots/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
