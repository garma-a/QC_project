info:
  name: "Update Profile"
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/users/me/profile"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
