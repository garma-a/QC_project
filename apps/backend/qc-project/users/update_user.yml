info:
  name: "Update User"
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/users/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
