info:
  name: "Delete User"
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseUrl}}/users/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
