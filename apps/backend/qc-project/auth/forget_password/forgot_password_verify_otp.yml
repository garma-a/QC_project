info:
  name: Forgot Password Verify OTP
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/auth/forgot-password/verify-otp"
  body:
    type: json
    data: |-
      {
        "email":"g@gmail.com",
        "otp":"199639"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
