info:
  name: Verify OTP
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/auth/signup/verify-otp"
  body:
    type: json
    data: |-
      {
        "email":"g@gmail.com",
        "otp":"525242"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
