info:
  name: Get Whitelist
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/auth/whitelist"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
