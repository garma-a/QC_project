the current state of this branch is that we are trying to implement complete working SSE
but for some reason the current state is that we implemented i broken SSE from  the frontend side
and working SSE from the backend side so when i open the frtonend UI and navigate to this route
http://localhost:3000/dashboard?machineId=22&tab=charts
i cannot see the graphs for this machine and also for any other machine so the data coming from the server is broken and we tried to fix it several times but faild so this is frontend arcitecture problem
that need a lot of debug to solve it and it is related to the new edits we implement on this branch for the SSE because it is working just fine on the main branch without the SSE code
also the qc route does not show the data also and this is related to the SSE update so there is alot of miss going on the frontend with the ability to fetch the data
for the initial  render then cache it and update the cache from the SSE when there is new data coming from the SSE route that we created
this is really hard problem as we tried to solve it several times and we fail so i will ask from you to analyise the syntax and search the intenet for the
root cause of this problem and try to create full report about how we can fix it so these requirements are met

1- the initial render of the charts and the qc route should be able to fetch the data from the server and show it on the UI
2- the SSE should be able to update the charts and the qc route when there is new data coming from the server without breaking the initial render and the ability to fetch the data
3- the SSE should be implemented in a way that it does not cause any performance issues on the frontend and it should be able to handle a large amount of data without crashing the UI or causing memory leaks
4- the SSE implementation should be compatible with the existing frontend architecture and it should not require any major changes to the existing codebase
5- the SSE implementation should be tested thoroughly to ensure that it works as expected and it does not cause any issues on the frontend
